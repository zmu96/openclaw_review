# ───────────────────────────────────────────
# AI Code Reviewer — requirements.txt
# Python 3.11+
# ───────────────────────────────────────────

# ── AI ──────────────────────────────────────
google-genai>=0.8.0               # Gemini API 클라이언트 (신버전 SDK: google.genai)
anthropic>=0.40.0                 # Claude API (1순위)
groq>=0.9.0                       # Groq 폴백용 (3순위)

# ── 웹 백엔드 ────────────────────────────────
fastapi==0.111.0                  # 웹 서버 프레임워크
uvicorn[standard]==0.30.1         # ASGI 서버
python-multipart==0.0.9           # 폼 데이터 파싱
jinja2==3.1.4                     # HTML 템플릿 렌더링
aiofiles==23.2.1                  # 비동기 파일 I/O

# ── 디스코드 봇 ──────────────────────────────
discord.py==2.3.2                 # 디스코드 봇

# ── 레포지토리 처리 ──────────────────────────
gitpython==3.1.43                 # git clone / 레포 분석

# ── 유틸리티 ─────────────────────────────────
python-dotenv==1.0.1              # .env 파일 로드
pydantic>=2.7.1,<3.0.0            # 데이터 검증 (FastAPI 내장)
httpx==0.27.0                     # 비동기 HTTP 클라이언트

# ── 테스트 ───────────────────────────────────
pytest==8.2.2
pytest-asyncio==0.23.7
